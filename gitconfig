# vim: ft=gitconfig
[user]
    email = mark.harviston@google.com
    name = Mark Harviston
[push]
    default = current
[cola]
    spellcheck = false
[core]
    editor = vim
[alias]
    autosquash = git rebase -i master --autosquash
    excludesFile = ~/.gitignore_global
    attributesfile = ~/.gitattributes
    ppush = push --force-with-lease
    bbranch = branch --sort=-committerdate
